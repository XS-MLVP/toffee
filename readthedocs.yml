version: 2

build:
  os: ubuntu-latest
  tools:
    python: "3.10"

python:
  install:
    - method: pip
      path: .
    - method: pip
      extra_requirements:
        - docs

sphinx:
  configuration: docs/source/conf.py
